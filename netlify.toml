[build]
  command = "corepack enable && pnpm run generate"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "22"
  NODE_OPTIONS = "--max-old-space-size=4096"
  COREPACK_ENABLE_DOWNLOAD_PROMPT = "0"
